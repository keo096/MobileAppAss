name: smart_quiz
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  device_preview: ^1.1.0
  carousel_slider: ^5.1.1
  provider: ^6.1.2
  intl: ^0.20.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # 🔹 Assets
  assets:
    - assets/images/

  # 🔹 Fonts
  fonts:
    - family: AKbalthom
      fonts:
        - asset: assets/fonts/AKbalthom Kbach Regular.ttf

    - family: SFPro
      fonts:
        - asset: assets/fonts/SF-Pro.ttf
